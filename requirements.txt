torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
# basicsr has a setup.py bug on Python 3.12+; install via patched source:
# git clone https://github.com/XPixelGroup/BasicSR.git
# patch setup.py get_version() to use exec(... , ns) then return ns['__version__']
# pip install ./BasicSR
basicsr==1.4.2
realesrgan==0.3.0
flask>=3.0.0
imageio-ffmpeg>=0.5.0
numpy>=1.24.0
pillow>=10.0.0
